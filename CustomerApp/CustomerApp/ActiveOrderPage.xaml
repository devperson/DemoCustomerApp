<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:CustomerApp.Controls;assembly=CustomerApp"
             Title="Current Order"
             x:Class="CustomerApp.ActiveOrderPage">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="0.7*"/>
      <RowDefinition Height="0.3*"/>
    </Grid.RowDefinitions>

    <controls:ExtendedMap x:Name="map" VerticalOptions="Fill" HorizontalOptions="Fill"></controls:ExtendedMap>

    <BoxView BackgroundColor="Red" Grid.Row="1" HorizontalOptions="Fill" VerticalOptions="Fill"></BoxView>

    <ContentView Grid.Row="1" HorizontalOptions="Center" VerticalOptions="Center" WidthRequest="180">
      <Label Text="You will receive a call when your driver is almost there." FontSize="17" TextColor="White" ></Label>
    </ContentView>
  </Grid>
</ContentPage>